using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// VR / UI Interaction Scene
/// 상호작용이 필요한 버튼 : Collider 씌우기, ButtonEvent 스크립트를 버튼의 컴포넌트로 넣기
/// 오른손 컨트롤러 : ControllerPointer 씌우기
/// </summary>

public class TestOutCode : MonoBehaviour
{
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void testPrint()
    {
        Debug.Log("TestOutCode.testPrint");
    }
}
